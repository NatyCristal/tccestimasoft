import 'package:flutter/widgets.dart';

class CardPadrapResultadosEstimativas extends StatelessWidget {
  final String nomeDaEstimativa;
  final Color backround;
  const CardPadrapResultadosEstimativas(
      {Key? key, required this.backround, required this.nomeDaEstimativa})
      : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [],
      ),
    );
  }
}
