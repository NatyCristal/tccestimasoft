import 'package:flutter/widgets.dart';

class EditarDadosUsuario extends StatelessWidget {
  const EditarDadosUsuario({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
